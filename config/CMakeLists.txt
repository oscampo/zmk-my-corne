# The BLE display feature (CONFIG_KBD_BLE_DISPLAY, renamed
# CONFIG_ZMK_COMPANION_DISPLAY) used to build straight from
# custom_status_screen.c/mono_*.c here. It's now an external west module
# (oscampo/zmk-companion, see config/west.yml and that repo's
# zephyr/module.yml), so this file has nothing left to add.
